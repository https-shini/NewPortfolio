/**
 * @deprecated
 * Componente legado — substituído por PositionEntry (acordeão inline).
 * Mantido apenas como stub para não quebrar referências antigas.
 * Pode ser removido com segurança em uma futura limpeza.
 */
import React from "react";

export const CareerPositionModal: React.FC = () => null;
