node_modules/
dist/
.vite/
.env
.env.local
.env.*.local
.DS_Store
Thumbs.db
*.log

# Artefatos de empacotamento do desktop — vão para a GitHub Release,
# nunca para o repositório (o AppImage sozinho tem 106 MB).
apps/desktop/release/

# Projeto Android gerado por `cap add android` — grande e derivado.
apps/mobile/android/
apps/mobile/ios/

# Saída intermediária da pré-renderização (scripts/prerender.mjs).
# O script a apaga ao terminar; isto cobre o build interrompido no meio.
.prerender/

# Documentos por rota — derivados do index.html em build.
# Ver `rotaHtmlPlugin` em apps/web/vite.config.ts.
apps/web/links.html
apps/web/release-notes.html
