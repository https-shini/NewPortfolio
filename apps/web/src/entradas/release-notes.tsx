/* Documento de `/release-notes` e de `/release-notes/vX.Y.Z`: as duas
   páginas entram aqui porque o mesmo documento serve as duas, e quem
   decide entre índice e versão é o roteador em cliente. */
import "@/pages/ReleaseNotes";
import "@/pages/ReleaseNote";
import { montar } from "./comum";

montar();
